% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex_sticker.R
\name{hex_sticker}
\alias{hex_sticker}
\title{Create a Hex Sticker}
\usage{
hex_sticker(
  name = "package",
  subtitle = NULL,
  url = NULL,
  icon = NULL,
  icon_image = NULL,
  icon_size = 1,
  theme = "stats",
  mode = "dark",
  bg = NULL,
  accent = NULL,
  text_color = NULL,
  border_color = NULL,
  sub_color = NULL,
  font_family = "mono",
  font_bold = TRUE,
  font_italic = FALSE,
  font_size = 12,
  border_width = 2,
  inner_border = TRUE,
  filename = NULL,
  width = 600,
  dpi = 300
)
}
\arguments{
\item{name}{Character. The package name displayed on the sticker.}

\item{subtitle}{Character or \code{NULL}. Optional subtitle shown below the
package name.}

\item{url}{Character or \code{NULL}. Optional URL shown near the bottom of the
sticker.}

\item{icon}{Character or \code{NULL}. Name of a built-in icon. See
\code{\link[=hexmakr_icons]{hexmakr_icons()}} for valid values. Ignored if \code{icon_image} is provided.}

\item{icon_image}{Character or \code{NULL}. Path to a custom image file (PNG or
JPEG). If provided, overrides \code{icon}.}

\item{icon_size}{Numeric. Size multiplier for the icon (default \code{1.0}).}

\item{theme}{Character. Theme name. One of \code{names(hexmakr_themes())}.
Default \code{"stats"}.}

\item{mode}{Character. Color mode, either \code{"dark"} (default) or \code{"light"}.}

\item{bg}{Character or \code{NULL}. Override the background color (hex string,
e.g. \code{"#1B2838"}).}

\item{accent}{Character or \code{NULL}. Override the accent color.}

\item{text_color}{Character or \code{NULL}. Override the text color.}

\item{border_color}{Character or \code{NULL}. Override the border color.}

\item{sub_color}{Character or \code{NULL}. Override the subtitle/URL color.}

\item{font_family}{Character. Font shorthand (e.g. \code{"mono"}, \code{"serif"},
\code{"sans"}) or a system font name. Default \code{"mono"}.}

\item{font_bold}{Logical. Use bold weight. Default \code{TRUE}.}

\item{font_italic}{Logical. Use italic style. Default \code{FALSE}.}

\item{font_size}{Numeric. Base font size in pt for the package name.
Default \code{12}.}

\item{border_width}{Numeric. Border line width in mm. Default \code{2}.}

\item{inner_border}{Logical. Draw a thin inner accent ring. Default \code{TRUE}.}

\item{filename}{Character or \code{NULL}. If provided, save the sticker as a
transparent PNG at this path using \code{\link[=save_hex]{save_hex()}}.}

\item{width}{Integer. Export width in pixels (height is derived from the hex
aspect ratio). Default \code{600}.}

\item{dpi}{Numeric. Resolution for PNG export. Default \code{300}.}
}
\value{
A ggplot object (invisibly if \code{filename} is not \code{NULL}).
}
\description{
Generates a publication-ready hexagon sticker for an R package.
Returns a ggplot object that can be printed, further customised with \code{+},
or saved with \code{\link[=save_hex]{save_hex()}}.
}
\details{
Create a hex sticker
}
\examples{
\donttest{
# Minimal sticker
hex_sticker("mypackage")

# With icon and theme
hex_sticker("mypackage", icon = "dna", theme = "genomics")

# Save to file
tmp <- tempfile(fileext = ".png")
hex_sticker("mypackage", icon = "atom", theme = "stats", filename = tmp)
file.exists(tmp)
}

}
\seealso{
\code{\link[=hexmakr_themes]{hexmakr_themes()}}, \code{\link[=hexmakr_icons]{hexmakr_icons()}}, \code{\link[=save_hex]{save_hex()}},
\code{\link[=hexmakr_app]{hexmakr_app()}}

Other stickers: 
\code{\link{hexmakr_app}()},
\code{\link{save_hex}()},
\code{\link{save_hex_svg}()}
}
\concept{stickers}
